# Devlog 2026-03-19 — Speaker Diarization & Audio-Synced Transcript

## What Was Done

### Speaker Diarization Pipeline
- Moonshine already outputs per-line speaker diarization (`speaker_index`, `has_speaker_id`, `start_time`) but we were discarding it
- Updated `scripts/moonshine_transcribe.py` to output JSON with speaker segments instead of plain text
- `src/transcribe.ts`: Added `SpeakerSegment` type, `parseMoonshineOutput()` (JSON with fallback to plain text), `formatSpeakerTranscript()` (merges adjacent same-speaker segments, includes `@seconds` timestamps)
- Verified on `steve.wav`: 264 segments, 5 unique speakers, 100% speaker_id coverage

### Speaker Name Resolution (3 strategies)
1. **Summary SPEAKER_MAP** — LLM returns `SPEAKER_MAP: Speaker 0=Name, Speaker 1=Name` in summary output. Works when the model follows instructions (worked on jensen-Illya.mp3)
2. **Heuristic extraction** (`extractNamesFromTranscript()`) — Detects address patterns like "Steve, when you began..." → next speaker = Steve. Also detects self-introductions. Instant, no LLM call needed. Primary strategy.
3. **Dedicated LLM call** (`resolveSpeakerNames()`) — Fallback focused call with hallucination filters (reject >40 chars, sentences, >5 words). Strips Qwen special tokens.

### Galaxy Dashboard UI
- **Two-column note modal**: Summary (left) + Audio/Transcript (right)
- **Speaker labels**: Purple pill badges with speaker names
- **Timestamps**: `0:02`, `3:38` etc. shown next to each line
- **Audio-transcript sync**: `timeupdate` highlights active line, auto-scrolls
- **Click-to-seek**: Click any transcript line to jump audio
- **User scroll priority**: Manual scrolling pauses auto-scroll for 4 seconds
- **HTTP Range requests**: Audio server supports `Accept-Ranges: bytes` + 206 Partial Content for seeking
- **Markdown summary rendering**: Headings, bullets, checkboxes, bold text rendered in summary panel

### Professional Summary Prompt
- Replaced simple TITLE/BRIEF/SUMMARY format with comprehensive meeting assistant prompt
- Output: About Meeting, Meeting Outline (topics with subtopics), Overview (conclusions), Todo List (action items)
- `sanitizeLlmOutput()` strips `<|endoftext|>`, `<|im_start|>`, trailing role labels
- Fixed `/note` endpoint regex to capture full markdown between `## Summary` and `## Transcript`

## E2E Test Results

### steve.wav (19:39, 5 speakers)
- Heuristic detected: Speaker 1 = Steve (from "Steve, when you began...")
- 55 conversation turns, timestamps throughout

### jensen-Illya.mp3 (5:07, 2 speakers)
- LLM resolved: Speaker 0 = Ilya Sutskever, Speaker 1 = Jensen Huang
- Full markdown summary with topics, overview, todo list
- Clean attendee line, no artifacts

## Commits
- `3970eea` — Speaker diarization, audio-synced transcript, two-column modal
- `4c5271d` — Dedicated LLM speaker name resolution with hallucination filtering
- `512a92e` — Heuristic speaker name extraction from transcript context
- `6ce9f2a` — Professional meeting summary prompt with markdown rendering
- `0f3eb37` — Fix summary parsing for full markdown content

## What's Next
- Speaker enrollment with pyannote embeddings (interface designed, not yet implemented)
- Better model compliance for structured output (consider switching from qwen3.5:9b)
- Test with real HiDock device recordings

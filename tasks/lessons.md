# Lessons Learned

## 2026-03-06
- Preventative rule: When adding a monitoring API, always ship and document at least one runnable production entrypoint (CLI/script/service wiring) in the same change; API-only monitoring is incomplete and likely to be silently unused.
- Preventative rule: Connection-state monitors must explicitly specify startup semantics (`emit on first connected` vs `transition-only`) and enforce them with unit tests.
- Preventative rule: Any outward notification path (Slack/DM/webhook) must be config-driven and injected behind a mockable sender interface so failures are non-fatal and behavior is unit-test deterministic.
- Preventative rule: When adding thread-aware messaging, always define and test explicit inactivity fallback routing (no-thread/main timeline) so alerts never silently stick to stale threads.
- Preventative rule: Any CLI module imported by another runtime path must guard entrypoint execution (`isDirectRun`) to avoid accidental side effects during watcher/daemon startup.
- Preventative rule: Any new remote storage backend must implement operation-level fallback to local persistence (`isIndexed` + save paths), so transient backend failures never block capture.
- Preventative rule: Backend selection should be normalized in one parser (`local|memdock`) and reused across manual + auto-sync flows to prevent drift in runtime behavior.
- Preventative rule: When a backend relies on HTTP endpoints, make API path prefix configurable (`*_API_PATH`) and test both env and CLI parsing to avoid hardcoded route coupling.
- Preventative rule: During naming/branding migrations, run a repo-wide string audit (`rg`) and classify each hit as rename vs compatibility-retain before editing; document intentionally retained legacy identifiers in `tasks/todo.md` final review.

## 2026-03-12
- Preventative rule: USB protocol parsers must not assume a fixed header format across device models. Always detect header markers and fall back to headerless parsing when absent.
- Preventative rule: Long-running servers (galaxy, watcher) must not restart on repeated events. Check if already running before recreating — repeated USB plug-in events are noisy and will wipe in-memory state.
- Preventative rule: All-or-nothing state persistence (only save on full success) causes infinite re-processing when any file fails. Save state incrementally per item.

## 2026-03-19
- Preventative rule: Don't rely on small local LLMs (qwen3.5:9b) for structured output compliance. They hallucinate names, ignore format instructions, and produce model artifacts. Always have a rule-based heuristic fallback for critical parsing (speaker names, metadata extraction).
- Preventative rule: When LLM output contains markdown with `## ` headings, note-parsing regexes that stop at `(?=\n## )` will break. Anchor to specific section boundaries (e.g., `## Summary` to `## Transcript`) not generic heading patterns.
- Preventative rule: LLM outputs from Qwen models leak special tokens (`<|endoftext|>`, `<|im_start|>user`). Always sanitize with a dedicated function that strips these artifacts — don't rely on `stripThinkTags` alone.
- Preventative rule: Ollama `stream: false` mode can hang indefinitely on long prompts or thinking models. Always use `stream: true` with incremental reading to avoid header timeouts.
- Preventative rule: Browser `<audio>` elements require HTTP Range request support (`Accept-Ranges: bytes`, 206 responses) for seeking. Without it, clicking the progress bar does nothing.

## 2026-04-07
- Preventative rule: When integrating a model loader from a third-party package (`get_model_for_X`), always inspect *which* model variant gets returned by default. `moonshine_voice.get_model_for_language("en")` defaulted to `MEDIUM_STREAMING` (live-latency tuned), not the `BASE` model the public benchmarks reported — running as default cost ~9× wall time for offline transcription. Pin the variant explicitly in code, never trust defaults for accuracy/perf-sensitive paths.
- Preventative rule: Don't trust an external benchmark's model selection without verifying the runtime defaults. asrbench's "moonshine/base 2.55% WER" measured `BASE`, but the same package call in production code can return a different arch silently.
- Preventative rule: HuggingFace-style auto-download paths can leave `.partial` files on network failure that block clean retry. When integrating any auto-downloader, surface partial-cleanup instructions in the user-facing error path.
- Preventative rule: Don't conflate "embedding model" names — `moonshine_voice.get_embedding_model()` returns a *text* embedding (`embeddinggemma-300m`) for intent recognition, not a speaker embedding. Always confirm embedding modality (audio/text/image) before designing around it.
- Preventative rule: Filename-based product ID labels in USB filter tables MUST be verified against live `ioreg`/`lsusb` output, not against folklore. The HiDock filter table had `0xb00d // P1` as a comment but the actual product IDs are `0xb00e=P1` and `0xb00d=H1E`. The wrong label caused the watcher to silently sync from the wrong device when both were on the bus.
- Preventative rule: NEVER swallow errors silently in periodic-poll catch blocks. The file-poll's `catch { /* device busy */ }` hid `LIBUSB_ERROR_NO_DEVICE` failures for hours of production runtime. At minimum, log the error reason while still suppressing the benign "no device found" case explicitly by name.
- Preventative rule: When an SDK that wraps a lower-level library hangs, drop down to the lower-level library and try the same operation. `webusb.requestDevice().open()` hung indefinitely on the H1E after a stalled USB transfer, but `usb.findByIds().open()` succeeded in 0.1s — the wedge was in the webusb wrapper, not libusb itself. A libusb-level `device.reset()` (~100ms) cleared the kernel state and let the webusb path succeed.
- Preventative rule: When mixing libusb-level and webusb-level access to the same device, **synchronize them** — never race. A fire-and-forget `nativeDevice.reset()` followed immediately by `webusb.requestDevice()` SIGSEGVs the libusb context. Always `await` the reset callback before passing the device handle to the higher-level wrapper.
- Preventative rule: Multi-device USB scenarios deserve dedicated enumeration logic. The default `webusb.requestDevice({ filters })` returns "the first match per filter" with no determinism across multiple matching devices. For multi-device hosts, enumerate via the underlying `usb.getDeviceList()` (which has no caching), sort by an explicit preference (or honor an env override), and pass the chosen vendor+productId to `webusb.requestDevice()` for the wrapper instance.
- Preventative rule: Decouple background data-collection loops from the side effects they trigger. The file-poll loop was wrapped in `if (runAutoSync)`, which meant `--no-auto-sync` disabled both file listing AND device-file UI updates. Always separate "gather data" from "act on data" so one can run without the other for diagnostics.
- Preventative rule: Browser tabs running WebUSB take **exclusive** control of the underlying device. If two processes (HiNotes Web in Chrome + the local watcher) try to claim the same HiDock, libusb returns `LIBUSB_ERROR_ACCESS` from `claimInterface()` for the loser. Document this in user-facing setup docs, surface the error from any silent catch path, and never assume a USB device is "free" just because no native process holds it.
- Preventative rule: Read loops that pull from a USB transport must scale their iteration cap by `expectedSize`, not use a fixed default. The `collectCommandBytes` loop in `client.ts` was capped at 4096 iterations × ~2-8 KB per read = ~8-32 MB max download, silently truncating every file larger than that with `Incomplete transfer for command=0x5`. The cap exists only as a sanity guard against infinite loops; the real hang protection is the per-iteration read timeout.
- Preventative rule: NEVER mark a failed file as "processed" in a sync-state store. The 2026-03-12 lesson "save state incrementally per item" applies to **successes only** — persisting failure as success causes the file to be permanently skipped on every subsequent sync, making the failure invisible and unrecoverable. `meetingsSync.ts`'s catch blocks were calling `markFileProcessed()` on every error, which caused today's recording to vanish from the candidate list after one truncated download. Failures must remain un-marked so the next sync retries them.
- Preventative rule: Periodic frontend data refresh must NOT terminate after the first successful fetch. The galaxy dashboard's `pollOnce()` called `clearInterval(pollTimer)` as soon as initial data arrived, freezing the page in time relative to backend mutations (device files, deletions, sync updates). Always run a slow background poll while the page is alive, with a cheap diff to suppress no-op re-renders.
- Preventative rule: Frontend bulk-update operations on rendered DOM (e.g. "rename all matching speaker labels") should key off a stable `data-*` attribute set at render time, NOT off the visible text content. Using text content makes the operation depend on render order and breaks if there's any escaping/whitespace difference between the rendered text and the canonical name.
- Preventative rule: Mutating server endpoints that rewrite user files MUST scope their regex to a specific section, not the whole file. The `renameSpeakerInTranscript` helper anchors its replacement to `## Transcript\n` so the summary, attendee list, and other prose containing the speaker name are left untouched. Always test "is the name mentioned in summary too?" before shipping.

## 2026-04-08
- Preventative rule: **For destructive/propagating user actions with ambiguous intent, ASK the user explicitly. Don't infer from heuristics.** The speaker rename feature went through three failed heuristics in one session: (a) "novel name → bulk" broke when an ad voice got misclustered into Lex Fridman's identity (renaming the ad to a new name nuked all real Lex lines); (b) "old name pattern (Speaker N → bulk, real name → single)" was less wrong but still required Shift+Enter overrides users would never discover; (c) the right answer was a modal popup with three explicit buttons (`Cancel · Just this line · All N lines`) shown only when there's real ambiguity (matchCount > 1). **Heuristics for user intent are unreliable** because the design space spans "name a diarized cluster" vs "fix one misdiarized line" and the same gesture means different things in different contexts. Don't try to be clever — ask.
- Preventative rule: When a "rename" operation involves multiple representations of the same identity (transcript label, summary text, meeting index, wiki entity, search index, in-memory data), the operation is fundamentally a **data migration**, not a single edit. Design it as a coordinator that walks all layers explicitly with per-layer counts in the response, not as a single-file find-and-replace. The four-layer rename in `src/speakerRename.ts` was the right shape; the original transcript-only helper was insufficient for the moment users could edit anything.
- Preventative rule: **Single-instance and bulk modes for the same operation should have DIFFERENT propagation scopes**, not just a different match count. Single-line fix on one transcript line MUST NOT touch the summary, meeting index, or wiki — the original speaker still exists for the unchanged lines, so wiki entities for that speaker remain valid. Touching them would partially destroy a valid identity based on one bad line. Bulk rename, on the other hand, MUST touch all derived state because the original identity is fully replaced. Wiring this required a `lineStart` parameter on the backend rename helper to distinguish "rewrite this exact line" from "rewrite all lines for this speaker".
- Preventative rule: When walking a directory tree to apply a textual transform, **track which files step 1 touched so step 2 doesn't re-process them**. The wiki rename's step 2 (text-replace inline mentions across all wiki files) was reading the destination people page that step 1 had just merged into, finding the literal `## Merged from "Speaker 2"` audit marker, and rewriting it to `## Merged from "Sean Song"`. Caught by tests because the merge case had explicit assertions for the marker text. Fix: maintain a `skipPeopleFiles` set populated in step 1.
- Preventative rule: **Word-boundary regex via `\b` is unsafe when the from-string ends in a digit** because `\b` is a word/non-word transition and digits are word characters. `Speaker 1\b` does NOT match in `Speaker 10` (good — it's a same-class boundary), but `Speaker (1)\b` won't match either because `)` is non-word and the next char is also non-word, so there's no transition. Use explicit non-word-class lookahead/lookbehind: `(?<![A-Za-z0-9_])from(?![A-Za-z0-9_])`. This works regardless of whether the from-string starts/ends with a word or non-word character.
- Preventative rule: **Backticks inside JS comments are LIVE inside a TypeScript template literal.** The galaxyHtml.ts file builds the served HTML/JS as a single TS template literal. Writing `// matches \`name\`` inside that JS section terminated the template literal at the first backtick, producing cryptic `TS1005: ';' expected` errors at lines that looked syntactically fine. Use straight prose in comments inside template literals, never markdown-style code-quotes.
- Preventative rule: **Frontend periodic refresh must NOT terminate after the first successful fetch.** The galaxy dashboard's `pollOnce()` called `clearInterval(pollTimer)` as soon as initial data arrived, freezing the page in time relative to backend mutations. Always run a slow background poll while the page is alive, with a cheap diff to suppress no-op re-renders. (This was today's "I don't see the recorder badges" symptom.)
- Preventative rule: **Test assertions for layered features must include the audit markers themselves**, not just the substantive content. The merge case's `## Merged from "Speaker 2"` marker was the canary that caught step 2's re-processing bug — without that explicit `expect(merged).toContain('## Merged from "Speaker 2"')` test, the silent rewrite would have shipped.
- Preventative rule: Build droppings from a mis-configured tsconfig (a base config that includes both src and tests with `rootDir: "."` writes to `dist/src/` and `dist/tests/`) accumulate silently across sessions. They don't break anything but pollute git status and confuse tracking. **Audit `dist/` periodically** for unexpected subdirectories and gitignore them defensively even after deletion.
